def text = "welocme to Hello World"
pipeline {
    agent any
    stages {
        stage ('Task') {
            steps {
            echo "${text}"
            scipt {
                    def name="vtech"
                    echo "${name}"    
}
            }
        }
    }
}
