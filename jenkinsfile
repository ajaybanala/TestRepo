def text = "welocme to Hello World"
pipeline {
    agent any
    stages {
        stage ('Task') {
            steps {
            echo "${text}"
            script {
                    def name="vtech"
                if (name == "ajay")
                println ("hi ${name}")
                else
                println ("secondloop")
                     
}
            }
        }
    }
}
